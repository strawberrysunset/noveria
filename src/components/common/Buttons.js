import React from 'react'
import styled from 'styled-components'
import { MdRefresh as RefreshIcon } from 'react-icons/md'

const Wrapper = styled.button`

`

export const RefreshButton = ({ icon, text }) => {
    return (
        <Wrapper>

        </Wrapper>
    )
}