import React from 'react'
import styled from 'styled-components'
import { Card } from '../../components'

const MarketsCard = styled(Card)`

`


// Get coins with highest marketcap and list them.

export const Markets = () => {
    return (
        <MarketsCard>

        </MarketsCard>
    )
}